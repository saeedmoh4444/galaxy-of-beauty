{
  "enabledMcpjsonServers": ["chrome-devtools", "expo", "next-devtools", "context7"],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{try{const j=JSON.parse(d);const f=(j.tool_response&&j.tool_response.filePath)||(j.tool_input&&j.tool_input.file_path);if(f)console.log(f)}catch(e){}})\" | { read -r f; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.json|*.md|*.css) npx prettier --write \"$f\" >/dev/null 2>&1 ;; esac; } || true",
            "timeout": 30,
            "statusMessage": "Formatting with prettier"
          }
        ]
      }
    ]
  }
}
