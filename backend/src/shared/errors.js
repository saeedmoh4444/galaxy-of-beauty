/**
 * Shared error classes and codes.
 * Re-exported from shared/ as the canonical import path.
 * Original location: src/utils/errors.js (now re-exports from here).
 */
export { AppError, ErrorCodes } from '../utils/errors.js';
