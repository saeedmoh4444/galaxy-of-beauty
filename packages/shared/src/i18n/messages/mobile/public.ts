// Mobile public screens (services, marketplace, technicians, guides).
// Populated by the mobile sweep agents.

export const mobilePublicMessages = {} as const satisfies Record<
  string,
  { ar: string; en: string }
>;
