// Public/marketing page strings (home, services, marketplace, search,
// blog, events, bundles…). Populated by the public sweep (X2).

export const marketingMessages = {} as const satisfies Record<string, { ar: string; en: string }>;
