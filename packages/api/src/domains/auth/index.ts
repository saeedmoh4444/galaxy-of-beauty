// ── Auth Domain ──
// Registration, login, token management, 2FA, password reset, user profiles
export { authRouter } from '../../routers/auth';
export { userRouter } from '../../routers/users';
export { uploadRouter } from '../../routers/uploads';
